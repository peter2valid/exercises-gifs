// Global app-level constants.
// Single source of truth — import from here, never hardcode these strings.

export const TENANT_ID  = 'default';
export const DEVICE_ID  = 'default'; // per-device override post-auth
